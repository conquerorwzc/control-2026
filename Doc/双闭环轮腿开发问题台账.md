# 双闭环轮腿开发问题台账

> 本文用于持续记录 `wheel_legged_demo_27` 在机构学、硬件接入、任务调度和真机调试中出现的问题。
>
> 更新规则：每个问题至少记录“现象、影响、根因、处理、验证、遗留风险”。解决后不删除，标记为“已关闭”，以免以后重复踩坑。
>
> **安全红线：** 只有明确允许“烧录 / 刷入 / 下载固件”时才能写入开发板。电机已使能或存在 PID、力矩、电流输出时，严禁通过 J-Link、GDB、断点、单步等方式暂停 MCU；紧急情况优先物理断电或经过统一安全入口发送停止命令。

**最后更新：2026 年 8 月 5 日**

## 1. 当前工程边界

| 项目 | 当前结论 |
| --- | --- |
| 工程 | `control-2026`，目标机器人为 `UserApp/robot/wheel_legged_demo_27` |
| 主控 | STM32H7 |
| 单腿机构 | 两个同心主动轴驱动的 ACE 显式 k 缩放平面五连杆；虚拟末端 C 按 J=C/k 映射为真实轮轴 J |
| 坐标系 | O 为髋点原点，x 向前/右为正，y 向下为正；J 为真实轮轴/虚拟腿末端 |
| 主动轴角 | phi1：前关节输入；phi2：后关节输入。具体电机方向、零位和链传动只在本车 `robot_config.h` 配置。 |
| 当前正式功能 | 读取四个关节与两个轮毂反馈、链传动换算、ACE FK/IK、真实末端 J 的雅可比、纯轮式纵向十维状态、固定 0.160 m 四输入 LQR、腿长 PID/重力前馈和六电机唯一输出仲裁 |
| 当前不做的事 | 不进行 IK 真机位置闭环输出、动态腿长 LQR、Kalman 融合或无人手扶的平衡验收 |
| 电机安全状态 | `chassis_output.c` 是六台电机唯一输出口。右拨杆只有中档允许闭环；上/下档、遥控离线、双拨杆下、反馈/FK/Jacobian/状态/VMC/PID 任一失效均零参考并 Stop。成功出力后失效会故障锁定，必须先拨离中档才能重置。调试器暂停 MCU 前必须先让电机 Stop。 |

### 1.1 当前代码分层

```text
RobotTask（约 1 ms）
    └── ChassisTask
        ├── 左腿：读取两个关节反馈 → 链传动换算 → ACE FK → 真实末端 J 雅可比
        ├── 右腿：读取两个关节反馈 → 链传动换算 → ACE FK → 真实末端 J 雅可比
        ├── chassis_state：轮端、IMU、两腿状态 → 纯轮式 s 的十维状态；髋点量仅作诊断
        ├── 左右腿：VMC 广义力命令 → 主动轴 / 电机轴目标力矩
        └── chassis_output：维持既有 Stop / Enable 仲裁

纯运动学层
    └── kinematics/parallel_leg.[ch]
        输入：phi1、phi2、ACE 几何参数、虚拟末端支路
        输出：虚拟 D/E/C、真实 H/J、腿长、腿角、真实末端 J 雅可比；并提供纯几何 IK

传动层
    └── joint_transmission.[ch]
        输入：电机累计角
        输出：主动轴角 phi1 / phi2

VMC 映射层
    └── chassis_vmc.c
        输入：leg.vmc.force_command、leg.vmc.pitch_torque_command 与真实末端 J 雅可比
        输出：phi1 / phi2 主动轴力矩、前后电机轴等效力矩；仅在显式小力矩测试开关打开时由输出层发送
```

原则：纯运动学模块不包含 CAN、电机对象、FreeRTOS、PID 或硬件句柄；左右腿、电机方向、零位、链轮齿数等差异仅放在腿配置中。

状态读取层
    └── chassis_state.c
        输入：左右轮端反馈、IMU、左右腿 FK、虚拟腿雅可比和主动轴速度
        输出：十维状态；其中 s、s_dot 是左右轮端平均滚动的纯轮式纵向里程计状态

## 2. 当前 ACE 机构与标定数据

所有长度单位均为 **m**。下面仅记录当前 `parallel_leg` 模型使用的配置；早期直接求真实 P 点的几何数据已经失效，不能再与当前变量窗口的 D/E/C/H/J 混用。

| 参数 | 当前值 | 含义与状态 |
| --- | ---: | --- |
| l0 | 0 | 两主动轴中心距；当前 ACE 同心模型固定为 0。 |
| AH | 0.105 | 真实第一杆长度。 |
| HJ | 0.125 | 真实末端杆长度。 |
| CE | 0.0625 | 虚拟五连杆被动杆长度。 |
| k=CE/HJ | 0.5 | 程序计算，禁止独立手填。 |
| AD=AE=kAH | 0.0525 | 虚拟两根主动杆长度。 |
| CD=CE | 0.0625 | 虚拟末端两根被动杆长度。 |
| virtual_end_branch_sign | -1 | 当前实物支路满足 cross(E-D, C-D)<0；需随装配或角度定义改变重新标定。不要用画面“左/右”代替叉积判据。 |

FK 的计算顺序是：由 phi1、phi2 求 D、E；两圆交点求 C；再用 J=C/k 得到真实轮轴。真实腿长和腿角均只由 J 计算：

$$
l=\lVert J\rVert,
\qquad
\theta_l=\operatorname{atan2}(-J_x,J_y)
$$

真实末端雅可比由虚拟雅可比缩放得到：

$$
J_J=\frac{1}{k}J_C
$$

VMC 只使用真实末端 J 导出的 [l, theta_l] 雅可比，不能在 VMC 中再次除以 k。

当前手动运动时观测到的腿长范围是 0.0666987002 m 至 0.187587872 m。这只是已覆盖的观测范围，并非最终安全工作范围或 LQR 调度范围。

关节零位、链轮齿数、链传动方向和 CAN ID 以本车 `robot_config.h` 为唯一来源。每个电机的输出方向与反馈方向应配对配置为 `normal/normal` 或 `reverse/reverse`，但其物理正方向仍必须通过“主动轴 phi 增大”的真机试验确认。

## 3. 已关闭问题

### P-001：反馈读取函数反复发送 `DMMotorStop()`

| 项目 | 记录 |
| --- | --- |
| 状态 | 已关闭 |
| 现象 | 原 `WheelLeggedJointStopAndGetAngle()` 在约 1 ms 的循环内先 Stop 电机，再读取累计角。 |
| 影响 | 即使只是查看 FK，也会持续修改电机输出状态；未来接入控制器时会与 Enable、PID、力矩指令冲突。 |
| 根因 | 把“安全输出动作”和“读取反馈”放进了同一函数。 |
| 处理 | 改为 `WheelLeggedJointGetAngle()`，仅读取 `motor->measure.total_angle` 并更新 `feedback_ready`。 |
| 当前结论 | `DMMotorStop()` 只在每个关节执行 `DMMotorInit()` 后调用一次；运行期的 FK 更新不写 CAN 输出状态。 |
| 后续约束 | 任何读取、估计、FK、IK 函数都不得隐式 Stop、Enable 或发送控制量。 |

### P-002：临时 PID 测试与零力矩逻辑抢同一电机

| 项目 | 记录 |
| --- | --- |
| 状态 | 已关闭 |
| 现象 | 测试时发现一侧 PID 参考偶尔为 0，另一侧一会儿为 0、一会儿不是 0，表现像多个逻辑在抢电机。 |
| 影响 | 达妙 CAN 发送任务异步运行时，可能交替发送 Stop、Enable、PID 参考，出现无力、抖动或不可预测的转动。 |
| 根因 | 临时测试路径中存在 `Stop → Enable → SetPIDRef`，而周期性发送与状态更新并非一个唯一出口。 |
| 处理 | 删除临时 `wheel_legged_pid_test.[ch]`，移除 Leg 对象内的临时 IK/PID 真机测试状态和写电机函数。 |
| 验证 | 当前 `RobotTask()` 只更新两腿反馈与 FK，不调用 `DMMotorEnable()`、`DMMotorSetPIDRef()`。 |
| 后续约束 | 一台电机任一时刻只能由一个“输出仲裁器/控制状态机”写命令；调试逻辑不能绕过该出口直接写电机。 |

### P-003：临时 IK/PID 调试逻辑污染正式腿对象

| 项目 | 记录 |
| --- | --- |
| 状态 | 已关闭 |
| 现象 | 为了临时验证 IK 和定腿长 PID，在正式 `Leg` 对象中加入了测试状态、斜坡、参考发送等逻辑。 |
| 影响 | 正式对象职责不清；测试结束后仍可能影响运行，并增加日后调试定位成本。 |
| 根因 | “计算运动学”和“驱动真机”的边界未分开。 |
| 处理 | 删除 `WheelLeggedLegInverseKinematicsTest_t`、`inverse_kinematics_test` 及相关函数；IK 保留在纯运动学模块内。 |
| 当前结论 | FK/IK 算法可离线调用；若未来要真机验证，应建立独立、显式使能、可一键退出的测试入口。 |

### P-004：以为 Stop 是“读取电机角度”的必要步骤

| 项目 | 记录 |
| --- | --- |
| 状态 | 已关闭 |
| 现象 | 曾通过 `WheelLeggedJointStopAndGetAngle()` 获取反馈，容易误以为不 Stop 就无法读角度。 |
| 影响 | 导致正常观测路径侵入执行器状态，甚至可能每周期重复发送零力矩。 |
| 根因 | 对电机“反馈通道”和“命令通道”职责混淆。 |
| 处理 | 将函数改名为 `WheelLeggedJointGetAngle()`，代码和命名均明确其只读职责。 |
| 后续约束 | `Stop` 是控制命令，不是传感器采样动作。 |

### P-005：FK / IK 与机械装配支路、零位标定混在一起理解

| 项目 | 记录 |
| --- | --- |
| 状态 | 历史记录，已由 P-009 的 ACE 模型替代；标定原则仍适用 |
| 现象 | 轮轴中心 P 的部分坐标曾出现偏差；需要区分几何公式错误、装配支路错误、链传动方向错误和电机零位错误。 |
| 根因 | 双闭环机构同一组边长存在多种圆交点；数学支路与实际装配支路必须一致。电机反馈角也不是天然等于主动轴角。 |
| 处理 | 将圆交点支路明确写入配置；用链传动模型将电机累计角换算到 phi1、phi2；分别记录左右腿电机机械零点。 |
| 验证 | 该记录对应旧 P 点模型，不能作为当前 ACE `ParallelLegInverseKinematics()` 的验收结果。当前 ACE 机构学主机回代测试已通过，仍需按 P-009 做真实 J 的多姿态实测。 |
| 后续约束 | 修改杆长、零点、齿数或装配方向后，必须重新做 FK→IK 回代和多姿态实测；当前模型只看真实末端 J，不可套用旧 P 点字段。 |

### P-006：底盘 component 逻辑误放到 robot 层

| 项目 | 记录 |
| --- | --- |
| 状态 | 已关闭 |
| 现象 | 在加入遥控命令与底盘任务时，曾将底盘对象、关节输出仲裁和 FK 调度放到 `wheel_legged_demo_27/robot.c` 附近。 |
| 影响 | 通用底盘能力会被绑死在一台车；修改左右腿 CAN、零位或杆长时容易同时改动 component 逻辑。 |
| 根因 | 未明确区分“机器人装配层”和“底盘 component 层”。 |
| 处理 | 将 `chassis.[ch]`、`chassis_leg.c`、`chassis_output.c` 移入 `components/chassis/chassis_wheel_legged_double_closed_loop`。robot 层只创建对象、初始化遥控器、传入本车配置并调度两个任务。 |
| 当前结论 | component 通过 `WheelLeggedChassisInitConfig_t` 接收配置，绝不包含任一具体机器人目录的 `robot_config.h`。 |
| 后续约束 | 与机构无关的控制、输出、安全状态机均进入 chassis component；CAN ID、零位、齿数、杆长、装配支路留在各机器人自己的 `robot_config.h`。 |

### P-007：VMC 计算与小力矩测试不得绕过输出仲裁

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-07-30 |
| 状态 | 已关闭（计算链路与受限输出路径）；真机符号测试未完成 |
| 现象 | FK 已可观测，但尚没有统一的“虚拟腿广义力 → 两个主动轴力矩”数据通道，后续接平衡控制时容易让上层直接写电机。 |
| 影响 | 若 VMC 计算与电机输出混在一起，调试器手填变量或后续控制器可能在未完成保护前直接驱动关节，存在安全风险。 |
| 根因 | 虚拟腿坐标、雅可比、链传动和电机输出仲裁原本没有明确的分层边界。 |
| 处理 | 在每条 `WheelLeggedLegInstance_t` 内增加 `vmc` 对象；纯机构学层实时给出虚拟腿雅可比；`chassis_vmc.c` 计算广义力到主动轴/电机轴的映射；`chassis_output.c` 作为唯一出口，仅当本腿 `torque_test_enable=1` 且 VMC 有效时，才写入测试力矩。 |
| 验证 | 主机单测覆盖左、右腿的站立、前伸、后伸姿态：解析雅可比与中心差分一致，并验证小扰动关系和虚功恒等式；结果为 geometry 9/9、transmission 3/3 通过。H7 整机编译通过。 |
| 遗留风险 | 当前测试仅有单台电机轴 ±2 N·m 绝对限幅，尚未实现变化率限幅、机械限位保护和失控超时保护；必须悬空、低值、单腿测试。未经明确授权不得烧录。 |

首次悬空符号测试的变量窗口操作顺序如下。右拨杆必须先处于使能位置，使 `chassis_mode=CHASSIS_ON`；但此时两腿默认仍为零力矩。

```text
1. robot->chassis->left_leg.vmc.force_command = 0
   robot->chassis->left_leg.vmc.pitch_torque_command = 0
   robot->chassis->left_leg.vmc.torque_test_enable = 0

2. 确认 left_leg.vmc.valid = 1，且两个 *_motor_torque_output 均为 0。

3. 先只填一个很小的命令，例如 F = +1 N、Tp = 0；
   确认两个 *_motor_torque 的计算值和两个 *_motor_torque_output 均不超过 ±2 N·m。

4. 人手扶住悬空腿、远离机械限位后，最后才将
   left_leg.vmc.torque_test_enable = 1。

5. 观察伸/缩腿趋势后，先把 torque_test_enable 改回 0，
   再修改 F、Tp 或切换到另一条腿。
```

`front_motor_torque`、`rear_motor_torque` 是未限幅的数学映射结果；`front_motor_torque_output`、`rear_motor_torque_output` 才是实际送给达妙的参考，代码会强制限制在 ±2 N·m。不要同时打开两条腿，也不要在电机使能时暂停、断点或单步 MCU。

虚拟腿的坐标约定固定为：O 是髋点，P 是轮轴中心，x 向前/右为正，y 向下为正。

$$
l=\sqrt{x_P^2+y_P^2}
$$

$$
\theta_l=\operatorname{atan2}(-x_P,y_P)
$$

其中 θ_l=0 表示轮轴在髋点正下方；θ_l 增大表示轮轴向 x 正方向移动、虚拟腿顺时针摆。`F>0` 表示伸腿，`Tp>0` 表示 θ_l 增大。每条腿独立用：

$$
\boldsymbol\tau_\phi=\mathbf J_v^\mathsf{T}
\begin{bmatrix}F\\T_p\end{bmatrix}
$$

计算主动轴力矩；再按各主动轴自己的链传动 `direction × gain` 换算为电机轴目标力矩。左右腿不在 VMC 层额外翻转符号。

### P-008：十维状态 s 曾错误地只使用纯轮式里程计

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-01 |
| 状态 | 已关闭（未处理轮打滑） |
| 现象 | 原 `s`、`s_dot` 仅由左右轮端转角和转速平均得到，腿前后摆动或伸缩时不能反映髋点 O 的真实纵向运动。 |
| 影响 | 十维模型中的位移、速度参考点与机身髋点不一致；后续以髋点为参考建模的 LQR 不能直接使用该状态。 |
| 根因 | 把轮轴中心由无滑滚动推得的位移，当成了机身髋点的纵向位移。 |
| 处理 | 状态层改为每条腿计算 `s_O = r * theta_w + l * sin(theta_leg)`，左右平均后记录零点；速度使用轮速、腿长变化和腿摆动三项完整求导。纯轮式聚合里程计字段不保留，轮端角和轮端速度仍作为髋点计算输入和调试量。 |
| 验证 | 主机单测覆盖腿竖直退化为轮式结果、纯腿摆动、纯腿长变化和非法输入拒绝；H7 工程仅编译，不烧录。 |
| 遗留风险 | 轮端滚动项仍假设无滑；轮滑、离地和轮径误差会污染髋点状态。Kalman 尚未接入，未来只能以髋点 `s/s_dot` 为估计器和 LQR 的输出参考。 |

### P-009：旧双闭环几何模型与当前 ACE 显式 k 模型混用

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-04 |
| 状态 | 已关闭（模型迁移）；几何尺寸仍需 CAD/实物复核 |
| 现象 | 旧文档和变量理解仍使用 A/B/C/D/E/F/P 的直接真实末端模型，而固件已切换为 ACE 显式 k 缩放模型，导致同名点、支路和腿长来源容易被误读。 |
| 影响 | 可能把虚拟点 C 当作真实轮轴，或对 Jacobian、IK、VMC 重复乘/除 k，从而造成腿长、角度或力矩比例错误。 |
| 根因 | 实物机构采用 ACE 等价五连杆描述；虚拟末端与真实末端不是同一个长度尺度。 |
| 处理 | 纯机构学改为由虚拟 D、E、C 求解，再保存真实 H、J；使用 k=CE/HJ、J=C/k。IK 输入真实 J 后先换算 C=kJ。真实末端雅可比以 J_J=(1/k)J_C 得到。 |
| 验证 | 主机 ACE 几何测试通过 10/10；传动测试通过 3/3；H7 Debug 工程编译通过，未烧录。 |
| 遗留风险 | AH、HJ、CE 与 AD/AH=CE/HJ 的平行四边形条件尚需 CAD/实物复核。若几何或支路修改，必须重新进行 FK 实测、IK-FK 回代和真实 J 雅可比中心差分测试。 |

### P-010：虚拟末端支路符号不直观，容易选到镜像轮轴

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-04 |
| 状态 | 已关闭（当前装配支路）；更改机构定义后需复核 |
| 现象 | `virtual_end_branch_sign` 只有 +1/-1，若不明确其几何含义，FK 会选择 D、E 两圆交点的另一镜像解。 |
| 影响 | C、J 可能落在髋点上方或非实际装配侧，造成腿长、腿角和 Jacobian 全部不对应实物。 |
| 根因 | 两圆交点天然有两个数学支路；符号不是电机方向，也不是链传动方向。 |
| 处理 | 规定按二维叉积选择 C 相对有向线 D→E 的一侧：cross(E-D, C-D)>0 对应 +1，小于 0 对应 -1。当前坐标 y 向下，代数叉积的正负不能直接称为画面“左/右”；实际选择必须以 FK 后真实 J 是否落在实物轮轴一侧为准。当前确认支路采用 -1。 |
| 验证 | 当前手动姿态下 J 位于预期物理侧，腿长覆盖 0.0666987002 m 至 0.187587872 m；仍需至少前伸、直立、后伸三姿态复核。 |
| 遗留风险 | 只凭单一姿态正确不能证明全行程无干涉或无跨支路；接近两圆相切时必须拒绝输出有效 FK/VMC。 |

### P-011：调试器中 FK 内部腿长偶尔显示为 0

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-04 |
| 状态 | 已定位；发布快照的连续性仍待真机长时间观察 |
| 现象 | 在 Ozone/J-Link 变量窗口中，`leg.kinematics.state.length` 偶尔出现一个采样点的 0，下一步又恢复正常。 |
| 影响 | 若直接用内部工作区画曲线，容易误判为机械腿长突变或 FK 失效。 |
| 根因 | `ParallelLegUpdate()` 每周期开始会清零内部 `state`，随后依次写 D、E、C、H、J、length 和 Jacobian；调试器异步采样可能恰好落在清零与完整写入之间。 |
| 处理 | 连续观测与控制使用 `chassis_state.left_leg_length`、`chassis_state.right_leg_length`，它们只在该帧完整 FK/Jacobian 有效时发布。内部 D/E/C/H/J 仅用于零力矩、停机后的逐步定位。 |
| 验证 | 观察到内部 0 后下一周期恢复，且发布状态不应据此判为同一腿跳零。尚未进行长时间趋势记录。 |
| 遗留风险 | 若发布状态也持续清零，应优先检查 `forward_kinematics_status`、`real_leg_jacobian_valid`、关节反馈和 `valid_mask`，不能把该现象一律归咎于调试器采样。 |

### P-012：`right_leg_length_dot` 的瞬时值难以直观看出正确性

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-04 |
| 状态 | 待真机低速验证 |
| 现象 | `right_leg_length_dot` 的单帧值看起来与手感不稳定或不直观。 |
| 影响 | 它参与髋点 O 的纵向速度；方向或比例错误会污染十维状态中的 `s_dot`。 |
| 根因 | 该量不是腿长的两帧差分，而是电机速度反馈经链传动转换为主动轴速度后，通过真实腿长雅可比即时求得；速度量化噪声和近奇异 Jacobian 均会放大抖动。 |
| 计算 | `length_dot = J_l(0,0)*phi1_dot + J_l(0,1)*phi2_dot`，其中 `phi_dot = transmission.direction * transmission.gain * motor.measure.velocity`；右前关节映射 phi1，右后关节映射 phi2，单位为 m/s。 |
| 处理 | 变量窗口同时查看右腿两个电机速度、两项 phi_dot、真实 Jacobian 第一行和 `right_leg_length_dot`；不允许在状态层为“看起来反向”而单独取负。 |
| 验证 | 需在零力矩、低速手动伸腿/缩腿时，用时间窗平均值和 Δright_leg_length/Δt 对比：伸腿应为正、缩腿应为负。单个约 1 ms 采样点不能作为方向结论。 |
| 遗留风险 | 若低速平均方向相反，按顺序检查前后 phi 槽位、链传动 direction、DM 反馈方向和真实雅可比；修正必须回到配置或机构学源头。 |

### P-013：旧双闭环模型残留且当前 IK 缺少板端无力验收入口

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-04 |
| 状态 | 已关闭（代码清理）；主机端 IK-FK 回代已通过 |
| 现象 | ACE `parallel_leg` 已是当前唯一机构学，但旧 `double_closed_loop_leg.[ch]` 仍留在目录与构建脚本的排除规则中。 |
| 影响 | 旧模型会误导后续维护。IK 需要有回归验证，但不应为一次性变量窗口验证长期保留底盘运行状态。 |
| 处理 | 删除旧 `.c/.h` 与 `robot.cmake` 的死路径。IK 仅保留 `ParallelLegInverseKinematics()` 接口和主机端 FK→IK→FK 回代测试；删除 chassis 级 `ik_debug` 运行状态和周期调度。 |
| 验证 | 主机单测已覆盖本车 ACE 参数 l0=0、AH=0.105 m、HJ=0.125 m、CE=0.0625 m、支路 -1 的真实 J 雅可比与三组代表性非奇异姿态 IK-FK 回代，结果通过；H7 Debug 构建通过，未烧录。 |
| 遗留风险 | IK 回代只证明几何与当前传动反馈自洽，不能证明机械尺寸绝对准确，也不能代替电机位置控制或 VMC/Tw 符号试验。 |

### P-014：腿长稳定需要与 VMC、关节输出仲裁明确分层

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 历史记录；已由 P-017 的四输入统一仲裁方案取代 |
| 现象 | VMC 已能把手填 F 映射为两个主动轴力矩，但尚未有闭环机制把实际腿长稳定在目标值。若让上层直接给关节角位置环，会绕开虚拟腿 F 的物理定义，且会和后续 LQR/VMC 争夺关节输出。 |
| 处理 | 每条 `WheelLeggedLegInstance_t` 增加独立 `length_control`。它只读取同一帧 FK/Jacobian 发布的 l、l_dot；PI 部分调用项目通用 `PIDCalculate(l,l_ref)`，而通用 PID 的 `Kd` 强制为 0。物理 D 项单独按 `F=limit(F_PI-velocity_damping_kd*l_dot)` 计算，再写入本腿 `vmc.force_command`。VMC 仍按真实 J 的雅可比把 F 映射为关节力矩，`chassis_output.c` 仍是唯一电机写入位置。 |
| 初始参数 | 参数只在 `wheel_legged_demo_27/robot_config.h` 中配置，左右腿一致。当前目标 l_ref=0.160 m，force_pid.Kp=500 N/m，force_pid.Ki=0，force_pid.Kd=0，velocity_damping_kd=5 N*s/m，force_pid.MaxOut 为唯一 F 限幅 ±20 N，允许腿长为 0.07 m 至 0.19 m。PID 与电机轴 ±2 N*m 限幅同时存在，前者限制虚拟腿广义力，后者限制每台 J4310 实际输出，二者不可互相替代。 |
| 有效性 | 仅当本腿 FK、真实腿雅可比、关节反馈和 PID 参数有效且当前腿长位于工作区时，`length_control.valid=1`。失效或越界时清空积分与 F；当前版本不再设置本腿人工 enable，只有 `CHASSIS_ON`、完整状态、LQR、VMC 和唯一输出仲裁器全部满足时才允许出力。 |
| 验证 | 主机单测直接链接真实 `controller.c`，以固定 1 ms 的 DWT 桩验证 PI 正负方向、积分限幅、Ki=0、Jacobian l_dot 阻尼、±20 N 限幅、运行时 MaxOut 同步、不合法腿长、无反馈、NaN 与非法 PID 配置；单腿悬空方向已由实机确认；H7 Debug 构建通过，本轮未烧录。 |
| 遗留风险 | 重力前馈、轮毂 Tw 和四输入仲裁已在 P-017 接入，但首次仍只能做手扶/受控台架小量试验；任何反向趋势都应先拨右杆到上/下档并检查 F/VMC 合同，不得用负 Kp 掩盖问题。 |

### P-015：LQR 导出常量曾缺少 MCU 调度边界和板端核对入口

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已关闭（影子计算已接入）；真实输出仍未接入 |
| 备注 | 该条中“二元二次腿长调度”和旧输入符号描述已被 P-016 取代；当前仅导出固定 0.160 m 的 K，Tw 合同为 `[-1,-1,+1,+1]`。 |
| 现象 | MATLAB 已生成 K、x_ref、u0 的二元二次腿长调度系数，但 MCU 没有状态误差与控制律计算，也没有一个能拒绝调度范围外腿长的统一入口。 |
| 影响 | 若直接把调度多项式外推到未建模腿长，或在没有同帧变量核对的前提下接入执行器，可能把错误 K、参考点或输入符号直接送到 VMC/轮毂。 |
| 处理 | 新增 `chassis_lqr.[ch]` 作为纯计算影子模块。它读取十维发布状态和左右腿长，使用 MATLAB 导出的 `[1,lL,lR,lL^2,lL*lR,lR^2]` 基底调度 K、x_ref、u0，并计算 `u=u0-K*(x-x_ref)`。MATLAB 导出头文件新增 0.140 m 至 0.180 m 的有效腿长边界；未采集状态零点、任一状态来源无效、NaN 或腿长越界时，本帧清零四路结果并令 valid=0。 |
| 输出边界 | 影子对象路径为 `robot->chassis->lqr`，顺序固定为 `[Tp_R,Tp_L,Tw_R,Tw_L]`。它不写 `leg.vmc.pitch_torque_command`、不调用 VMC/轮毂输出函数，也不调用 Stop、Enable、SetRef 或 PID 接口。MATLAB 已将 Tp 的 `input_sign=[-1,-1,1,1]` 合并进 K，MCU 绝不二次取反。 |
| 验证 | MATLAB 重新计算 81 个工作点并完成一致性验证；主机回归为 geometry 14/14、transmission 3/3、hip_odometry 11/11、length_control 28/28、shadow_lqr 17/17；H7 Debug 编译通过，未烧录。 |
| 遗留风险 | Tw 正方向仍需真机验证，且尚无轮毂输出仲裁、Tp 到 VMC 的正式接入、总力矩/变化率限幅、故障锁定或完整控制模式状态机。因此影子 LQR 通过不等于可以进行落地平衡测试。 |

### P-016：纯轮式 s 下的机身动力学参考点与 Tw 接触力定义

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已完成模型修正与数值核验；真实 H6215 方向仍待单轮低幅试验 |
| 现象 | 旧 MATLAB builder 直接维护五条已化简方程，并把左右腿的 q 二阶导数平均后同时代入左右轮。这会在左右腿非对称时抹掉 q_R-q_L 对 Yaw 的影响；同时旧 Tw 既被描述为“轮对腿反作用力矩”，又被当作“轮端前进力矩”，导致 `input_sign` 与文档相互矛盾。 |
| 当前 s | 十维状态正式使用纯轮式平均滚动坐标 `s=R*(theta_wL+theta_wR)/2-s0`。髋点 O 的 `s_O` 只保留作固件诊断量，不能和当前 K 混用。 |
| 处理 | `build_symbolic_model.m` 从 17 条原始 Newton-Euler 残差自动消去 12 个接触内力，再提取 `M(q)*ddq=B(q)*u+g(q,dq)`。纯轮式约束使用 `R*ddtheta_wL=ddot_s-b*ddot_phi+(q_R_dd-q_L_dd)/2`、`R*ddtheta_wR=ddot_s+b*ddot_phi+(q_L_dd-q_R_dd)/2`；同时补上机身水平加速度 `a_bx=ddot_s+(q_L_dd+q_R_dd)/2`，避免再次把轮端坐标当成髋点坐标。 |
| Tw 合同 | MATLAB/MCU 影子变量中的 `Tw_i>0` 固定为“对应轮端向前滚动的力矩”。`Tw_R>0` 应使右轮角向前增加、`phi_ddot>0`；`Tw_L>0` 应使左轮角向前增加、`phi_ddot<0`。`input_sign=[-1,-1,+1,+1]`：前两项仅处理 VMC 的 Tp 正方向，后两项不再额外取反。H6215 电机轴正指令到该轮端定义的映射仍待低幅单轮标定。 |
| Tw 与接触力 | `Tw` 是轮电机对轮子的力矩；`F_g_to_w` 是地面对轮的接触力；`F_w_to_l` 是轮对腿的轴承/轮轴力。当前模型保留三者独立方程。两轮正 Tw 的标称数值检查得到地面对轮的水平摩擦力为正；轮对腿的力由相对加速度决定，不能固定写成永远向前。 |
| LQR 验证 | MATLAB 已验证 B 的正 Tw 方向、左右轮角/Yaw 方向、内力消元、纯轮式约束、可控性 `10/10`、闭环极点和导出头。当前完整无约束 LQR 的 `s>0` 初始 `-Kx` 仍可能给正 Tw；这是四输入耦合反馈的结果，不能直接在 MCU 末端取负。 |
| 遗留风险 | 当前等法向力、平地、纯滚动、双轮接地、固定 0.160 m、忽略 l_dot/l_ddot 的边界不变。`Tw` 到 H6215 轴命令的实测映射、完整工作点标定和真实闭环稳定性仍未验收。 |

### P-017：四输入 LQR 必须通过唯一六电机仲裁器接入

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已接入，未烧录、未完成真机手扶验收 |
| 现象 | 先前 LQR 只计算调试量，腿长 PID、VMC 和轮毂分别存在人工开关，无法安全地将 `[Tp_R,Tp_L,Tw_R,Tw_L]` 同周期接入，也会让多个模块争夺电机状态。 |
| 处理 | 删除 `lqr.enable`、`length_control.enable`、`vmc.torque_test_enable`。右拨杆仅中档请求闭环；`chassis_output.c` 成为六台电机唯一的 Stop、Enable、`DMMotorSetRef` 出口。Tp 在写入 VMC 前限幅为 ±0.10 N*m，Tw 直接按 1:1 轮端力矩写入对应 H6215，限幅 ±0.20 N*m、变化率 1.0 N*m/s。输出已成功使能后若反馈、FK/Jacobian、状态、PID、VMC、LQR 或数值任一失效，即零参考 Stop 并锁定；必须先将右拨杆离开中档再回中档才能重新使能。 |
| 重力前馈 | 每腿保存 `force_pid_command`、`gravity_feedforward` 和最终 `force_command`。在完整状态和有效投影下，`F_ff=1.683*9.81/(cos(theta_L)+cos(theta_R))`；投影小于 0.20、无效或非有限时清零 F 并阻止出力。直立时约为 8.25 N/腿，最终 F 仍限制在 ±20 N。 |
| 验证 | 主机端 geometry 14/14、transmission 3/3、hip_odometry 11/11、length_control 34/34、LQR 23/23 均通过；H7 Debug 在隔离目录编译通过，未烧录。 |
| 遗留风险 | H6215 正电机命令到“对应轮向前”的方向仍必须先手扶低幅验证。当前 K 只适用于双腿约 0.160 m、平地、纯滚动、双轮接地；手扶小量试验只验收方向、限幅和急停，不能代替保护架平衡验收。 |

### P-018：十维状态 s 改回纯轮式平均里程计

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已完成代码和 MATLAB 合同切换；未烧录、未进行真实四输入闭环 |
| 原因 | 髋点 O 里程计把腿角摆动和腿长变化也混入 s，手推前进时不容易直接用轮端正方向判断状态极性；当前 LQR 需要先固定为最直观的左右轮平均滚动坐标。 |
| 新定义 | `s_w=R_w*(theta_wL+theta_wR)/2`，`s=s_w-s_origin`；`s_dot=R_w*(wheel_speed_L+wheel_speed_R)/2`。左右轮的 direction 和减速比换算在状态模块之前完成。 |
| 状态影响 | 轮子不动时摆腿或改变腿长不会改变 `state_vector[0]`、`state_vector[1]`；左右轮向前滚动时 `s_dot` 直接为正。Yaw 不参与 s 的 cos/sin 投影。 |
| 有效性 | 新增 `WHEEL_LEGGED_STATE_VALID_WHEEL_ODOMETRY`。纯轮式计算要求左右轮反馈和两个轮半径有效；原点仍只在左右轮、IMU、左右腿全部有效时捕获。完整 LQR 状态要求该轮式有效位，不要求髋点有效位。 |
| 诊断保留 | `wheel_odometry_s_raw`、`wheel_odometry_s_dot` 用于状态观察；`hip_odometry_s_raw`、`left_hip_odometry_s_dot`、`right_hip_odometry_s_dot` 保留用于和髋点估计对比，不参与十维状态或输出仲裁。 |
| MATLAB 同步 | 左右轮约束改为 `R*theta_wL=s-b*phi+(q_R-q_L)/2`、`R*theta_wR=s+b*phi+(q_L-q_R)/2`；相加得到纯轮式 s，但左右 q 差分仍保留在 Yaw 方程。已修改 builder、验证脚本、导出 Markdown 和参数说明，必须重新生成 A、B、K、x_ref、u0 后才能使用。 |
| 验证 | 新增主机纯轮式里程计测试，覆盖两轮位置/速度、不同轮径、零速度、非法半径和 NaN；MATLAB 验证覆盖约束位置、一阶/二阶导数及左右 q 差分。H7 仅编译，不烧录。 |
| 遗留风险 | 纯轮式 s 假设无滑，轮滑、离地、轮径偏差仍会造成累积误差；Kalman 尚未接入。后续若改回髋点参考或接入滤波器，必须重新推导并重新生成 K，不能混用当前纯轮式 K 与 body/髋点版状态。 |

### P-019：不能用单个 LQR 输入符号判定完整闭环恢复方向

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已确认数学边界；真实工作点和全状态闭环仍待验收 |
| 现象 | 当前标称模型中 `B(2,Tw_R)>0`、`B(2,Tw_L)>0`，即正 Tw 使纯轮式 `s` 的加速度增加；但完整 LQR 对单独 `s>0` 的初始 `-Kx` 可能输出正 Tw。 |
| 根因 | 标准无约束 LQR 同时压制 s、腿角、机身 Pitch 等状态，四个输入通过动力学耦合共同优化。单个状态扰动的第一拍输入不保证每个执行器都对该状态直接反向；当前线性闭环极点仍为稳定。 |
| 处理 | 验证脚本新增闭环 B 方向、内力分离和接触力数值检查；禁止把 `Tw` 直接当摩擦力或轮对腿力，也禁止在 MCU 输出端仅因 `s>0` 初始 Tw 为正就手动取负。 |
| 验证 | 标称纯轮式模型可控性为 `10/10`；`Tw_R>0`、`Tw_L>0` 分别使对应轮角加速度为正，且使 `s_ddot` 为正、`phi_ddot` 分别为正和负。零加速度受力定义检查中 `F_g_to_w`、`F_w_to_l` 均为正；真实两轮动态响应的 `F_g_to_w` 为 `+10.868173 N`，`F_w_to_l` 为 `-4.275216 N`，这是轮子平动惯性项造成的状态相关结果。MATLAB 生成与符号验证通过。 |
| 遗留风险 | 实机必须先完成原点、腿角工作点、H6215 正方向和单轮低幅试验。若控制目标确实要求 `s>0` 时每一拍 Tw 都负，需要另行设计带结构约束的外环/输入分配器，不能伪装成 LQR 输入符号修复。 |

### P-020：参考模型内部扭矩与电机 Tw 的合同必须显式分开

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-08-05 |
| 状态 | 已加入符号模型和 MATLAB 自动验证 |
| 问题 | SJTU/WBR 资料中的 `T_wl_to_l`、`T_wr_to_r` 是轮对腿的内部反作用扭矩；本项目 LQR 输入 `Tw_R/Tw_L` 定义为电机施加给轮子的轮端力矩。两者若直接同名使用，会造成参考模型、K 和 H6215 输出方向混淆。 |
| 统一关系 | `T_wheel_to_leg = -Tw_motor`。轮转动方程保留电机 Tw，腿转动方程使用定子对腿的反作用扭矩。 |
| 处理 | `build_symbolic_model.m` 显式保存左右轮对腿反作用扭矩；`verify_double_closed_loop_lqr.m` 检查该恒等式、Tw 到轮角/s/Yaw 的方向以及接触力分离。 |
| 重要边界 | 当前四输入无约束 LQR 的公共 Tw 到纯轮式 s 存在右半平面不变零点，因此单独 `s>0` 或 `s_dot>0` 时第一拍 Tw 不保证为负。不能因此在 MCU 末端取反；若必须第一拍反向，应另行设计受约束外环或输入分配器。 |
| 遗留风险 | H6215 电机轴正指令到“轮端 Tw 正方向”的实物映射仍需保护架单轮低幅试验确认。 |

## 4. 当前风险与待解决问题

### R-001：已有唯一电机输出仲裁器，但尚未形成完整安全状态机

当前 Stop、Enable 与 VMC 小力矩参考都只经过 `chassis_output.c`；但未来接入 PID、力控、保护、调试、急停后，仍应扩展为完整状态机。建议状态至少区分：失能、零力矩、位置测试、力矩/电流控制、故障锁定。

### R-002：轮毂、IMU、遥控器只接入读取或安全链路，尚未形成整车控制

轮毂已接入反馈读取和髋点纵向里程计输入，IMU 已接入姿态读取与十维状态组装，遥控器已接入关节使能/失能和急停。它们尚未接入轮毂闭环、纵向状态估计、平衡控制或云台控制；因此不能据此认为整车可运动或可平衡。

### R-003：达妙反馈有效标志的判定仍需确认

当前 `feedback_ready` 使用 `measure.state != 0u` 进行粗略判断。后续应确认达妙协议中各状态值的含义，明确哪些状态代表正常反馈、失能、故障或离线。

### R-004：VMC 已有受限真机输出，但完整安全保护仍未完成

目前板端实时计算：主动轴角 → 真实末端 J 的腿长/腿角与雅可比，VMC 命令 → 主动轴/电机轴等效力矩。每条腿仅在 `chassis_mode` 非断电、`torque_test_enable=1`、`vmc.valid=1` 时允许经唯一输出入口进行悬空小力矩符号测试，且单台电机轴输出被限制为 ±2 N·m；默认仍为 0 N·m。后续还需要：

1. 为达妙确认纯力矩模式、力矩单位和可持续允许范围。
2. 在唯一 `chassis_output` 出口补充变化率限幅、机械限位保护、失控超时和故障锁定状态机。
3. 对雅可比接近奇异、FK 无效、反馈失效和腿长超工作范围实现强制降额/清零。
4. 悬空、小力矩、物理限位充分避让的条件下，逐项确认左右腿 F、Tp 的力矩符号。
5. 以已接入的 IMU、轮速和腿部状态建立纵向状态估计后，再接入 LQR / 平衡控制。

### R-005：工程仍存在一个既有编译告警

当前已知告警为 `IMU_LeverArm_Compensation declared static but never defined`。它与本轮腿部 FK/IK 修改无直接关系，但后续应单独定位并消除，避免真正的遗漏被告警淹没。

## 5. 后续开发顺序

1. **冻结并回归验证纯机构学层**：用前伸、直立、后伸等多姿态测量真实末端 J，记录 FK 误差；维护 `parallel_leg_test` 的 IK-FK 回代和雅可比中心差分验证。
2. **完成统一输出仲裁器**：所有 Stop、Enable、PID、力矩指令必须只从一个模块发出。
3. **建立安全测试模式**：测试模式默认失能，只有显式命令才能启用；退出、超时、反馈异常时回到零力矩或失能。
4. **标定轮毂和 IMU**：确认 CAN 通道、反馈、方向、轮径、安装姿态和单位，再建立状态估计。
5. **完成雅可比与力映射验证**：先离线数值差分，再做低风险静态真机检查。
6. **完成 LQR 就绪审计后再接入平衡控制**：先接入十维状态误差、参考工作点、K 的输入顺序和 `input_sign`；再建立左右 H6215 的唯一 Tw 输出仲裁及完整力矩安全状态机。此前不得测试完整四输入 LQR。

## 6. 新问题记录模板

复制下列模板追加到“已关闭问题”或“当前风险与待解决问题”中：

```markdown
### P-XXX：问题名称

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-XX-XX |
| 状态 | 待分析 / 处理中 / 已关闭 |
| 现象 | 复现条件、日志、观察到的变量。 |
| 影响 | 对安全、机构学精度或功能的影响。 |
| 根因 | 已确认原因；未确认则写假设和待验证项。 |
| 处理 | 做了哪些代码、参数或流程调整。 |
| 验证 | 构建、离线计算、真机测试的结果。 |
| 遗留风险 | 仍未覆盖的条件和下一步。 |
```
